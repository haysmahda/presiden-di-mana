# dependencies
node_modules/

# secrets — never commit API keys
.env
.env.*
!.env.example

# private working documents — keep these local, never publish
CLAUDE.MD
PROJECT-STATE.md
local/

# local Claude Code settings (machine-specific)
.claude/settings.local.json
.claude/*.tmp.*

# os / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# logs
*.log
npm-debug.log*
