# dependencies
node_modules/

# secrets — never commit API keys
.env
.env.*
!.env.example

# the project brief is a private working document — keep it local
CLAUDE.MD

# local Claude Code settings (machine-specific)
.claude/settings.local.json
.claude/*.tmp.*

# os / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# logs
*.log
npm-debug.log*
